<header class="shadow-sm">
  <nav class="navbar navbar-expand-lg navbar-light">
    <div class="container">
       <a class="navbar-brand p-3" href="/"><%= link_to 'WelcomeGamers!', '/', class: 'navbar-brand' %></a>
      <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav ml-auto">
        <% if user_signed_in? %>
          <li>
            <%= link_to "投稿フォーム", new_post_path, class: "btn btn-outline-secondary btn-sm" %>
          </li>
          <li>
            <%= link_to "マイページ", user_path(current_user.id), class: "btn btn-outline-secondary btn-sm" %>
          </li>
          <li>
            <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "btn btn-outline-secondary btn-sm" %>
          </li>
        <% else %>
          <li>
            <%= link_to "新規登録", new_user_registration_path, class: "btn btn-outline-secondary btn-sm" %>
          </li>
          <li>
            <%= link_to "ログイン", new_user_session_path, class: "btn btn-outline-secondary btn-sm" %>
          </li>
        <% end %>
        </ul>
      </div>
    </div>
  </nav>
</header>