<h1>募集一覧ページ</h1>

<% @posts.each do |posts| %>
<div>
  <%= link_to post_path(posts.id) do %>
    <p>募集タイトル</p>
    <%= posts.title %>
    <p>募集文</p>
    <p>ユーザーネーム：<%= posts.user.name %></p>
    <p>投稿日：<%= posts.created_at.strftime('%Y/%m/%d') %></p>
    <p><%= posts.caption %></p>
  <% end %>
</div>
<% end %>