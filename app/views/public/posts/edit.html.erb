<h2>編集画面</h2>
<%= form_with model: @post, url: post_path(@post.id), method: :patch do |f| %>
  <h4>募集タイトル</h4>
  <%= f.text_field :title %>
  <h4>募集文</h4>
  <%= f.text_area :caption %>

  <%= f.submit '保存' %>
<% end %>