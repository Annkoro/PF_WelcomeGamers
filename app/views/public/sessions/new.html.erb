<div class="row mt-5">
  <div class="col-6 offset-3">
    <h2 class="text-center mb-4"><i class="fas fa-sign-in-alt"></i>ログイン</h2>

    <div class="notification">
      <h5 class="notice"><%= notice %></h5>
    </div>

    <%#= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <%= form_with model: @user, url: user_session_path do |f| %>
      <div class="form-group">
        <div class="field">
          <%= f.label :email, "メールアドレス" %><br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
        </div>
      </div>
        <!--<div class="field">-->
        <!--  <%#= f.label :name %><br>-->
        <!--  <%#= f.name_field :name, autofocus: true, autocomplete: "name" %>-->
        <!--</div>-->
      <div class="form-group">
        <div class="field">
          <%= f.label :password, "パスワード" %><br />
          <%= f.password_field :password, autocomplete: "current-password", class: "form-control form-control-lg" %>
        </div>
      </div>
      <div class="form-group">
        <% if devise_mapping.rememberable? %>
          <div class="field">
            <%= f.check_box :remember_me %>
            <%= f.label :remember_me, "ログインを記録する" %>
          </div>
        <% end %>
      </div>
      <div class="form-group">
        <div class="actions">
          <%= f.submit "ログインする", class: "btn btn-lg btn-primary btn-block mb-3" %>
        </div>
      </div>
    <% end %>
    <div>アカウントの登録がお済みではない方はこちらより<%= render "public/shared/links" %>を行ってください。</div>
 </div>
</div>